// (function () {
//     function init() {
function search(textElement) {
    if (event.key == 'Enter') {
        console.log(textElement.value);
    }
}
function createListOrGroup() {
    if (document.getElementById("group")) {
        
    }
}

    //     }
//     init();
// })();